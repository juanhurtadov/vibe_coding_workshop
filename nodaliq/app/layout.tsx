import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ClerkProvider } from "@clerk/nextjs";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NodalIQ — AI Battery Dispatch Intelligence",
  description:
    "Stop leaving money on the table. NodalIQ uses agentic AI to optimize your battery dispatch schedule against real ERCOT market prices — every day, automatically.",
  keywords: [
    "battery storage optimization",
    "BESS dispatch",
    "ERCOT",
    "energy arbitrage",
    "AI energy",
    "grid storage",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
          {children}
        </body>
      </html>
    </ClerkProvider>
  );
}
